# API Configuration
# Base URL for the backend API
# Relative URL uses Vite dev proxy (recommended for local dev)
VITE_BACKEND_URL=http://localhost:5002

# Other Configuration
VITE_APP_NAME=Smart University Platform
VITE_APP_VERSION=1.0.0
