# Security Audit Remediation Report

Audit date: 2026-09-03

Session scope: SEC-07 through SEC-40, excluding SEC-13, SEC-14, SEC-24, and SEC-33.

Execution order: the priority order supplied for this session.

The commit-hash cell for the item being committed says `this commit` because a Git commit cannot contain its own hash. The exact immutable hash is available from the matching commit subject in `git log`; completed earlier rows will be backfilled when a later item updates this report.

| SEC-ID | Status (Fixed/Partial/Skipped) | Files changed | Commit hash | Notes/assumptions |
| --- | --- | --- | --- | --- |
| SEC-22 | Fixed | `.dockerignore`; `Dockerfile`; `scripts/docker-context-security.test.mjs`; `CODEX-OVERNIGHT-REPORT.md` | `ab7e37c` | Docker context is default-deny and re-admits only required manifests, TypeScript source, Prisma schema, and active migration files. Builder and runner copies are explicit; root `.npmrc`, local env files, uploads, tests, seeds, archived migrations, and whole builder filesystems are excluded. Static exploit canaries, local API build, and `tsc --noEmit` pass. Docker is unavailable on this host, so a real image/layer inspection remains a CI verification step. Existing migration commands were preserved but no migration or database operation was run. |
| SEC-35 | Fixed | `artifacts/api-server/src/app.ts`; `artifacts/api-server/tests/health_security.test.ts`; `CODEX-OVERNIGHT-REPORT.md` | `6e6ce8ee` | Public `/api/health` and `/api/healthz` are rate-limited, dependency-free liveness aliases returning only `{ status: 'ok' }`. Detailed `/api/health/readiness` is rate-limited and restricted to active, current-session `SUPER_ADMIN` users; failures return a generic 503 and log details server-side. Focused live HTTP tests cover exact public shape, absence of public/unauthorized DB queries, 401/403/200/503 paths, error redaction, and 429 limiting. `tsc --noEmit` passes. Redis remains optional when unconfigured, preserving the existing policy. |
| SEC-38 | Fixed | `artifacts/api-server/src/index.ts`; `artifacts/api-server/src/server.ts`; `artifacts/api-server/src/utils/jwtSecretValidation.ts`; `artifacts/api-server/tests/jwt_secret_validation_security.test.ts`; `CODEX-OVERNIGHT-REPORT.md` | `ee1c73e9` | The actual esbuild/Docker entry point and the legacy server now share a side-effect-free validator that rejects missing, undersized, and known-default secrets without echoing configured values. Tests cover all known defaults, case/whitespace variants, the length boundary, and a legitimate strong value. The production bundle exits with code 1 for a known-default secret before listening. API build and `tsc --noEmit` pass. |
| SEC-39 | Fixed | `Dockerfile`; `scripts/docker-context-security.test.mjs`; `CODEX-OVERNIGHT-REPORT.md` | `123eaf2` | Builder installation now requires the committed lockfile. The final image uses the base image's unprivileged `node` user; only `/app/uploads` is made application-writable for existing local upload behavior. Static regression checks reject lockfile drift and root runtime execution. `tsc --noEmit` passes. Docker is unavailable locally, so a real container build/user/write-path smoke test remains a CI step. |
| SEC-26 | Fixed | `artifacts/api-server/src/routes/college.routes.ts`; `artifacts/api-server/src/controllers/college.controller.ts`; `artifacts/api-server/src/services/college.service.ts`; `artifacts/api-server/src/utils/scope.utils.ts`; `artifacts/api-server/tests/college_public_projection_security.test.ts`; `artifacts/university-app/src/services/college.service.ts`; `artifacts/university-app/src/pages/colleges/CollegesList.tsx`; `artifacts/university-app/src/pages/registration/AdminsList.tsx`; `CODEX-OVERNIGHT-REPORT.md` | `7a5b8fd1` | Public list/detail responses select only `id`, `name`, and `nameAr`. Counts, descriptions, department trees, and assigned-admin identifiers/emails moved to authenticated `/manage` routes with fail-closed college scope for super, legacy, college, and department admins. Administrative UI consumers use the protected routes; registration and dropdown consumers retain the minimal public list. Tests prove the public Prisma projection cannot fetch sensitive fields and cross-tenant detail IDs are intersected with scope rather than substituted. API and frontend `tsc --noEmit` pass. |
| SEC-29 | Fixed | `artifacts/api-server/src/controllers/exams.controller.ts`; `artifacts/api-server/tests/exam_metadata_scope_security.test.ts`; `CODEX-OVERNIGHT-REPORT.md` | `a6878c7` | Exam-by-ID now intersects the requested ID with `getScopeWhere(user, 'exam')` inside the Prisma query instead of attempting to interpret only admin-shaped course predicates after loading the record. Tests cover college admin, department admin, doctor, TA, student, and an out-of-scope 404 without returning metadata. `tsc --noEmit` passes. |
| SEC-34 | Fixed | `artifacts/api-server/src/app.ts`; `artifacts/api-server/src/middleware/requestLimits.middleware.ts`; `artifacts/api-server/src/utils/requestLimits.ts`; `artifacts/api-server/src/routes/quiz.routes.ts`; `artifacts/api-server/src/routes/exams.routes.ts`; `artifacts/api-server/src/routes/attendance.routes.ts`; `artifacts/api-server/src/routes/schedules.routes.ts`; `artifacts/api-server/src/controllers/schedules.controller.ts`; `artifacts/api-server/tests/request_limits_security.test.ts`; `CODEX-OVERNIGHT-REPORT.md` | `12d6e37a` | Every `/api` route now rejects non-decimal, non-positive, non-safe, duplicate/structured pagination values and unsafe page numbers before controller/Prisma execution; otherwise-valid limits above 100 are clamped to 100 for compatibility with existing list consumers. The page ceiling is 1,000,000. Quiz/exam answers are capped at 200 entries and 4,000 characters per answer; anti-cheat logs at 500 entries with bounded type/details/timestamp fields; manual attendance at 500 records with 1,000-character remarks; and grid sync at 500 slots with bounded text fields plus a controller-level array guard. Focused boundary and alternate-shape tests, `git diff --check`, and `tsc --noEmit` pass. |
| SEC-07 | Fixed | `artifacts/api-server/src/controllers/transcript.controller.ts`; `artifacts/api-server/src/utils/transcriptScope.utils.ts`; `artifacts/api-server/tests/transcript_overview_scope_security.test.ts`; `CODEX-OVERNIGHT-REPORT.md` | `5eeef01e` | The no-student-ID overview is now explicitly limited to super/legacy/college/department administrators and doctors. Unauthorized roles fail before any overview query. Exam predicates use `getScopeWhere(user, 'exam')`; quiz and task predicates constrain their parent course with `getScopeWhere(user, 'course')`, so every query is tenant/assignment scoped and missing managed scope fails closed. Tests cover denied roles, department and college tenants, assigned doctors, unscoped legacy admins, and super admins. `tsc --noEmit` passes. |
| SEC-08 | Fixed | `artifacts/api-server/src/controllers/courses.controller.ts`; `artifacts/api-server/src/controllers/students.controller.ts`; `artifacts/api-server/src/controllers/department.controller.ts`; `artifacts/api-server/src/utils/adminMutationScope.utils.ts`; `artifacts/api-server/tests/admin_mutation_scope_security.test.ts`; `CODEX-OVERNIGHT-REPORT.md` | `1b6d594b` | Course create/update, student creation, and department create/update/delete now use one fail-closed admin mutation predicate. Legacy, college, and department admins must have their role-specific managed ID; profile `collegeId`/`departmentId` fallbacks are deliberately rejected. Existing resources and destination department/college parents are each queried together with scope before mutation, blocking cross-tenant creates and moves. Focused controller tests prove cross-scope course/student/department attempts stop before writes; scope-shape tests cover every admin role. `tsc --noEmit` passes. |
| SEC-09 | Fixed | `artifacts/api-server/src/controllers/courses.controller.ts`; `artifacts/api-server/src/routes/courses.routes.ts`; `artifacts/api-server/tests/course_material_access_security.test.ts`; `CODEX-OVERNIGHT-REPORT.md` | `de27bb2a` | Course publication and material management now default-deny. Doctors and TAs require an actual schedule-slot assignment to the exact course; department membership is not a substitute. Legacy/college/department admins resolve the course with the shared fail-closed tenant predicate, while only super admins remain global. Upload authorization and ID validation now run before the file parser, preventing rejected users from creating orphaned uploads; the controller retains its second authorization check. Tests cover in/out-of-scope admins, missing managed scope, assigned/unassigned staff, invalid roles/IDs, and middleware order. `tsc --noEmit` passes. |
| SEC-10 | Fixed | `artifacts/api-server/src/controllers/courses.controller.ts`; `artifacts/api-server/src/routes/courses.routes.ts`; `artifacts/api-server/tests/course_roster_privacy_security.test.ts`; `CODEX-OVERNIGHT-REPORT.md` | `3bd27e00` | Student course detail queries no longer include the enrollment relation, so peer IDs, names, emails, profile data, group details, and enrollment rows cannot enter the response. The roster route has an explicit non-student role gate and then reuses the exact-course assignment/tenant authorization from SEC-09; students and unassigned staff stop before any roster or attendance query, including for cohort-visible courses. Tests inspect the Prisma projection and prove denied paths do not query sensitive tables. `tsc --noEmit` passes. |
| SEC-27 | Fixed | `artifacts/api-server/src/controllers/search.controller.ts`; `artifacts/api-server/src/utils/searchScope.utils.ts`; `artifacts/api-server/tests/search_scope_security.test.ts`; `CODEX-OVERNIGHT-REPORT.md` | `dea55f79` | Global search now has an explicit allowlist for super/legacy/college/department admins, doctors, and TAs. Every student, doctor, course, college, and department query combines its text predicate with `getScopeWhere` output via `AND`; missing managed IDs or staff profiles and all undefined roles are rejected before queries. Tests cover department and legacy admins, doctor/TA scope shapes, unconfigured admins, denied roles, and all five runtime query predicates. `tsc --noEmit` passes. |
| SEC-28 | Fixed | `artifacts/api-server/src/controllers/dashboard.controller.ts`; `artifacts/api-server/src/controllers/analytics.controller.ts`; `artifacts/api-server/src/utils/administrativeAnalyticsScope.utils.ts`; `artifacts/api-server/tests/administrative_analytics_scope_security.test.ts`; `CODEX-OVERNIGHT-REPORT.md` | `918f217f` | One validated administrative-scope bundle now drives every dashboard and general-analytics aggregate and the dashboard cache key. Student/staff/course/department/college/payment/exam/attendance/risk/recent-item queries are tenant-filtered; nested college distribution also filters its departments. Admin counts use tenant-aware user scope and super-admin totals are hidden from non-super admins. Missing managed scope is rejected before cache or database access. Tests exercise all analytics aggregates and the dashboard's college, user, exam, and nested-department predicates. `tsc --noEmit` passes. |
| SEC-16 | Fixed | `artifacts/api-server/src/controllers/schedules.controller.ts`; `artifacts/api-server/src/utils/scheduleAssignment.utils.ts`; `artifacts/api-server/tests/schedule_assignment_security.test.ts`; `CODEX-OVERNIGHT-REPORT.md` | `baeb5a5d` | General slot creation now resolves doctor/TA callers to their authenticated staff identity and requires an existing exact course assignment inside the serializable mutation transaction. Supplied staff IDs for admin-created slots must also already be assigned. Updates require assignment proof whenever the course or attached staff changes, and owning doctors/TAs cannot change either staff attachment; those changes must use scoped administrative flows. Focused tests cover unassigned doctor/TA creation, cross-staff mutation denial, doctor ownership transfer denial, and an admin's unassigned replacement doctor. `tsc --noEmit` passes. |
| SEC-17 | Fixed | `artifacts/api-server/src/controllers/doctors.controller.ts`; `artifacts/api-server/src/controllers/teachingAssistants.controller.ts`; `artifacts/api-server/src/utils/adminMutationScope.utils.ts`; `artifacts/api-server/tests/staff_course_admin_scope_security.test.ts`; `CODEX-OVERNIGHT-REPORT.md` | `28346db7` | Doctor and TA assignment/unassignment now query both the target staff member and course through exact, fail-closed managed-tenant predicates. Only super admins remain unscoped; legacy/college admins require `managedCollegeId`, and department admins require `managedDepartmentId` rather than profile fallbacks. Unassignment also embeds both tenant predicates in the delete itself. Positive integer course IDs are required. Focused tests cover both staff types, both tenant levels, missing managed scope, denial before mutation, and scoped delete predicates. `tsc --noEmit` passes. |
| SEC-18 | Fixed | `artifacts/api-server/src/controllers/studentGroups.controller.ts`; `artifacts/api-server/src/services/studentGroups.service.ts`; `artifacts/api-server/src/utils/studentGroupScope.utils.ts`; `artifacts/api-server/tests/student_group_scope_security.test.ts`; `CODEX-OVERNIGHT-REPORT.md` | this commit (`fix(security): scope student group operations`) | Group listing and department-tree queries now intersect caller scope: strict managed tenant scope for admins, the student's own department/year, or exact department/year pairs from staff course assignments. Unknown, incomplete, and unassigned identities receive a contradictory predicate. Auto-division, splitting, deletion, and recursive descendant lookups resolve their target through strict admin scope. Manual reassignment scopes both student and group and verifies matching department/year inside one transaction. Subgroups now inherit the parent's year instead of silently defaulting to year 1. Focused tests cover read predicates, staff assignment-derived scope, split/delete denial before mutation, scoped trees, and cross-year reassignment denial. `tsc --noEmit` passes. |

## Assumptions and remaining risks

- SEC-22: Existing production startup/migration behavior was preserved for compatibility; this session did not execute it.
- SEC-22: The API and its referenced workspace libraries currently use TypeScript source files only (apart from two inert `.gitkeep` files), so the Docker source allowlist admits `.ts` files and excludes unknown file types by default.
- SEC-22: Docker is not installed on the host. The regression check validates build-context rules and copy boundaries, and the local production bundle builds successfully, but final image contents and layers still need inspection in Docker-enabled CI.
- SEC-22: The current unrelated dependency work includes a package that declares a newer Node engine than the Docker base image; this may independently affect a future Docker build and was not changed under this finding.
- SEC-35: `SUPER_ADMIN` is the conservative in-repository interpretation of internal operational access because no mTLS, internal-network, or dedicated probe-secret middleware exists. Public deployment probes retain the cheap liveness aliases.
- SEC-39: The official Node Alpine image's existing `node` account is used instead of creating another UID. Runtime dependencies and migrations remain read-only; `/app/uploads` is the only application-owned writable directory.
- SEC-34: A page-number ceiling of 1,000,000 is a conservative guard against unsafe/expensive Prisma offsets; no legitimate university dataset is expected to require a larger page index. Malformed pagination is rejected with the existing 422 validation response, while valid limits above 100 are clamped to preserve current list-screen behavior.
- SEC-07: Teaching assistants were excluded from the aggregate transcript overview because the current product route exposes that view to doctors and administrative roles, not TAs. Their ability to retrieve an explicitly identified in-scope student's transcript is unchanged.
- SEC-08: Scoped administrators cannot create a course without a department or detach a course from its department, because an unparented record has no enforceable tenant ownership. `SUPER_ADMIN` retains the existing ability to detach during an update.
- SEC-10: The student-safe detail retains only the aggregate enrollment count; no row-level peer identity is loaded. Full roster and attendance remarks remain available only to assigned staff and in-scope administrators.
- SEC-28: Super-admin accounts are institution-wide rather than tenant-owned, so their count is returned only to `SUPER_ADMIN`; scoped tenant administrators receive zero instead of a potentially identifying partial count.

## Not reached yet

SEC-19, SEC-20, SEC-30, SEC-21, SEC-12, SEC-15, SEC-25, SEC-36, SEC-37, SEC-32, SEC-23, SEC-31, SEC-11, and SEC-40.

Explicitly out of scope: SEC-13, SEC-14, SEC-24, and SEC-33.
